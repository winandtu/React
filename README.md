# React

### Para crear un proyecto en react (si no lo tiene):
npx create-react-app my-app

### Una vez que el comando finalice, navega al directorio del proyecto:
cd my-app

### Para iniciar el servidor de desarrollo de React, ejecuta el siguiente comando:
npm start
